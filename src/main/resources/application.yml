#配置数据源(注意格式)
spring:
  datasource:
    #    mysql5(注意pom)
    driver-class-name: com.mysql.jdbc.Driver
#    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
      #    password: root
    password: daibin
    url: jdbc:mysql://localhost/test?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowMultiQueries=true
#配置日志级别
logging:
  level:
    com.example.plus.mapper: debug
#mybatis plusxml路径配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
